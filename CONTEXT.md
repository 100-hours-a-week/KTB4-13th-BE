# 도메인 용어

## 주소지

- **활성 주소지**: `EntityStatus.ACTIVE` 상태이며 회원의 주소지 목록·기본 배송지 후보에 포함되는 주소지입니다.
- **삭제된 주소지**: 삭제 요청으로 `EntityStatus.DELETED` 상태가 된 주소지입니다. 기존 행은 보존하지만 활성 주소지로 취급하지 않습니다.
- **기본 배송지**: 회원의 활성 주소지 중 기본으로 선택된 주소지입니다. 활성 주소지가 없으면 기본 배송지도 없습니다.
- **최근 주소지**: 같은 회원의 다른 활성 주소지 중 `created_at`이 가장 최신인 주소지입니다. 생성 시각이 같으면 `id`가 큰 주소지를 우선합니다.

## 주소지 삭제

주소지 삭제는 물리 삭제가 아닌 상태 전이입니다. 삭제 대상이 기본 배송지이면 같은 트랜잭션에서 최근 활성 주소지를 기본 배송지로 승격하고, 승격할 주소지가 없으면 기본 배송지를 비워 둡니다.
